#include <stdio.h>
#define n 10
int queue[n];
int front = -1;
int rear = -1;
int queue(int data)
{
    if(rear == -1 && rear == -1){
        printf("Queue is empty");
    }else{
        if(front == 0 || rear == 0)
    }
}
